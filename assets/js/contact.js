$(function(){
/*
// #tab1以外を非表示にする
$('.contact div[id != "tab1"]').hide();

//タブをクリック
$(".form_tab a").click(function(){
    $('.contact div').hide();

    //href属性を取得
    $($(this).attr("href")).show();

    //現在のcurrentクラスを削除
    $(".current").removeClass("current");

    //選択されたタブにcurrentクラスを追加
    $(this).addClass("current");

    return false;
});
*/

});

